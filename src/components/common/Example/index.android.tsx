import React from 'react';
import { Text } from 'react-native';
import { ExampleProps } from './types';

function Example(_props: ExampleProps) {
  return <Text>Example on Android</Text>;
}

export default Example;
