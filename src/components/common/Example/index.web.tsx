import React from 'react';
import { Text } from 'react-native';
import { ExampleProps } from './types';

function Example(_props: ExampleProps) {
  return <Text>Example on Web</Text>;
}

export default Example;
